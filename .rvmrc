rvm use default@chef-hatch-repo-0.1.3 --create

if ! command -v knife ; then
  gem install chef --no-rdoc --no-ri --version 0.10.8
fi

if ! command -v vagrant ; then
  gem install vagrant --no-rdoc --no-ri --version 1.0.0
fi
